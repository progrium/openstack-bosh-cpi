=== 0.0.5 - 2012-12-05
* Use stemcell version & name when present
* Store stemcell name & version at image properties
* Fix 'wait_resource'
* Fix network configuration
* Upgrade bosh_cpi to v0.5.1
* Allow CPI tasks to be cancelled
* Upgrade rspec & guard gems
* Updated CPI documentation
* Fix availability zones in create vm and create disk
* Added info messages when uploading kernel and ramdisk images
* Added support for OpenStack regions
* Use separate glance parms
* Add logger accessor
* Fix 'wait_resource' not returning symbols
* Fix 'wait_resource' when resource is not found
* Fix delete settings from registry
* Handle resources not found
* Handle server not found when rebooting
* Added basic integration tests for VM lifecycle
* Added single quotes to resource names
* Log stemcell_id and instance_type when creating a new vm
* Log time taken to create images
* Cosmetic changes for log messages at create_vm
* Add configure_network test to VM integration tests
* Comestic changes: wrap code at 80 columns
* Change how args are passed to httpclient

=== 0.0.4 - 2012-11-01
* Use "latest" metadata endpoint
* security groups are already put into hashes at this point, so without…
* empty map returns an enumerator, but it should just be the array
* Upgrade bosh_common and fog gems
* Fix create_vm with security groups unit test

=== 0.0.3 - 2012-08-15
* Add Authors file
* Fix image look-up
* Fix Floating IP association
* Refactor create_stemcell
* Return all Cloud IDs as Strings
* Added @vamins to AUTHORS
* Update bundled fog gem
* Fix create_stemcell unit spec (string<->symbol)
* Delete unused copy_root_image method and has_stemcell_copy tests
* Fix create_stemcell
* Upgrade fog gem to version 1.5.0
* Remove files from git (.gitignore)
* Add glance to BOSH OpenStack CPI console
* Added a new method to upload images
* Add support to kernel_file and ramdisk_file stemcell options
* Delete kernel and ramdisk when deleting the stemcell
* Upgrade some gems: bosh_common, bosh_cpi, excon, httpclient, uuidtools